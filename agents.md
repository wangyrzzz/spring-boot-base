# AGENTS.md
本文件为agent在此仓库中工作时提供指导。

# 项目开发说明

当前项目是一个开发脚手架项目。

进行代码调整时，不需要考虑历史兼容性，只需要保证当前需求下的逻辑闭环、代码可编译，并通过相关测试。

## 重要规则

1. **生成代码时保证代码结构扁平化。主流程显式展开，同一个类中连续业务步骤，优先保留在一个主方法中，同一类内，为组织连续业务步骤而抽取的辅助方法，调用深度最多两层，5行以内逻辑简单的复用代码不抽取公共方法；不计框架、工具类及正常跨层调用。主流程显式展开，避免只做转发的方法。**
2. **避免在一行代码中内联多个方法调用；避免在一次流程中重复遍历，同一阶段、同一集合上的独立校验尽量合并；依赖批量查询结果的校验允许分阶段执行。避免循环查询数据库，以及为减少循环而牺牲可读性。**
3. **编写查询接口返回时，避免在sql中写入大量逻辑，没特殊申明时以编号查询都使用精确匹配，没特殊声明时不需要解析返回状态枚举值对应的名称**
4. **禁止在代码中使用魔法值，需要根据字段含义为其定义枚举、常量值或错误码**
